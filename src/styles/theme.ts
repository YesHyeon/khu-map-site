import { css } from '@emotion/react';

const colors = {
  burgundy: '#9E1915',
};

const typography = {
  semibold01: css`
    font-size: 24px;
    font-weight: 600;
    line-height: 130%;
  `,
};

const theme = {
  colors,
  typography,
};

export default theme;
